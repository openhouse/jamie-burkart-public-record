# Evaluations

`npm run check` rebuilds generated indexes, validates source and link contracts,
then proves evaluator sensitivity against adversarial mutations. Green checks
do not authorize publication, quotation, photography, or portfolio placement.
